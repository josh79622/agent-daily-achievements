# 通用每日成就工具 — 專案簡述

> 2026-09-15 與 Josh 討論後整理。這份文件記錄產品目標與已確認的範圍，**不是技術設計或實作計畫**。尚未決定的事不應被 agent 自行定案。

## 要解決的問題

Josh 常在一天結束時覺得自己不知道做了什麼，因此容易自責和焦慮。他在求職、學習與其他專案中，大部分事情會與 AI agent 討論或請 agent 協助執行。這些紀錄分散在不同工具和專案裡，回頭逐一翻找很費力。

這個工具要每天從有來源可查的紀錄中，整理出實際做過的事、取得的進展、做出的決定，以及學到的內容，讓 Josh 能看見當天的成果。它是**跨領域**工具，不依附於求職或學習助理。

## 已確認的使用情境與範圍

1. **先服務這一台 Mac，首次公開發佈只支援 macOS。** 跨這台電腦上的多個專案讀取資料；第一版不需要雲端伺服器。Windows、Linux 或雲端支援留待後續決定。
2. **涵蓋五類來源的能力。** 主工具讀取本機的 Claude Code 與 Codex 紀錄，但使用者可能只安裝其中一個；任一個單獨存在時主工具都應可使用。可選的 Chrome 擴充功能增加網頁版 Claude、ChatGPT、Gemini 的新對話。Josh 交替使用 Claude Code 與 Codex。
3. **Chrome 擴充功能是可選的 add-on。** 不安裝它也能使用本機來源；安裝後只收集從開始使用擴充功能之後的新網頁對話，不匯入安裝前的歷史。每個網站能收集到什麼，需要實測。
4. **每天自動產生報告，並主動發出 Mac 通知。** 如果排定時間電腦正在睡眠，醒來後要能補做，不把那天直接略過。
5. **先用本機網頁介面供 Josh 查看與修正報告。** 這只是這台 Mac 上的介面，不代表使用雲端服務。先完成第一版的功能，再把 Mac App 視為後續階段；不能假設網頁介面會自動轉成 App。報告要能回到原始資料，讓 Josh 知道每項內容從哪裡來。
6. **這應是其他人可從 GitHub 下載並安裝使用的工具。** 首次公開發佈以 macOS 為目標。主工具先採 Node.js LTS 執行環境與 GitHub 原始碼加設定命令的方式，不要求獨立執行檔；設定流程應檢查必要條件，缺少時提供安裝指引，並完成相依套件與本機背景工作的設定。Josh 希望使用者能請自己的 AI agent 執行少量命令。可選的 Chrome add-on 有獨立的安裝步驟。具體命令仍待設計與驗證；不要把「少量命令」當成已實現的承諾。
7. **摘要可使用外部 AI，但每位使用者須在設定時明確同意。** 若只有 Claude 可作摘要，就交給 Claude；若只有 ChatGPT／OpenAI 可作摘要，就交給 OpenAI；若兩者都可用，優先交給 OpenAI。這指摘要服務的可用性，不是依對話紀錄來自哪個工具決定。對話內容在同意前不得傳送，也不得在失敗時默默改用另一家。何謂「可用」、如何以使用者帳號或其他方式自動呼叫、同意涵蓋的資料範圍仍待設計與驗證。

## 什麼可以寫成「成就」

成就不只等於完成待辦。有效內容也可以是：推進了一件事、釐清一個問題、做了有理由的決定，或真的學會了一個概念。摘要應描述具體事情，不用沒有根據的稱讚代替事實。

資料來源和結論要分清楚。對話裡說「打算做」不等於已做完；網頁對話若沒有可查的執行證據，不應宣稱任務已完成。不同來源提到同一件事時，要避免重複計算。Josh 可以修正或刪除錯誤項目。未安裝的本機 agent 應標示為未安裝，可選的 add-on 沒有啟用時，網頁來源應標示為未啟用；兩者都不當成讀取失敗或當天沒有活動。已啟用或已安裝的來源讀取失敗時，報告應標示資料不完整。

## 第一版的完成判準

在這台 Mac 上，Josh 不必每天手動翻查各專案紀錄。工具能收集上述來源中可取得的新資料，依本機日期自動產生一份有原始來源可查的報告，主動通知他，並讓他在本機介面確認、修正內容。至少要實測五類來源各自的收集與失敗情況；不能把尚未實測的網站支援寫成已完成。

使用者不安裝 add-on 時，主工具仍須能用本機來源產生報告；只安裝 Claude Code 或只安裝 Codex 時也須可用。第一版包含可選 add-on 的能力是否也須與主工具同時公開發佈，仍待確認；這不改變上段對五類來源能力的驗收要求。

最早的可運作版本可以先接通較少來源，作為端到端骨架；**那不是上段所定義的第一版完成**。每增加一個來源都要有可觀察的驗收結果。

GitHub 發佈與他人可安裝是產品目標，不是為了追求 GitHub stars 而增加無關功能。公開發佈前應在乾淨的 macOS 使用者環境驗證安裝，不只在開發者的既有 checkout 上運作；具體驗證環境和安裝步驟仍待設計。

## 明確不做

- 不把求職管理、學習助理或教授文章管理整進這個工具；那些是獨立的專案，日後可以互通。
- 不在第一版建立雲端帳號、雲端伺服器或跨裝置同步。
- 不回溯匯入 Chrome 網頁對話的歷史資料。
- 不將沒有來源或只有 AI 推測的內容，當作 Josh 已完成的成就。
- 不為了展示 AWS、GraphQL 或追求 GitHub stars 而改變這個工具的第一版目標。這些是 Josh 對其他側專案或公開作品的期待，不是本工具的功能要求。

## 動工前仍須決定或驗證

- 第一版應用程式程式碼已選 TypeScript（本機工作程式、網頁介面與可選的 Chrome add-on），主工具執行環境已選 Node.js LTS，初期安裝方式是 GitHub 原始碼加設定命令。框架、套件管理、儲存、具體安裝流程，以及本機網頁介面、背景產生報告與 Mac 通知如何分工仍待決定；Mac App 留待後續階段。
- 摘要服務優先順序及須明確同意外傳已定；仍須設計實際呼叫方式、模型、使用者可同意的資料範圍、停止同意後的行為，以及失敗時如何標示報告。
- 每個來源的紀錄格式、可用欄位、權限與保留方式；Chrome 三個網站的收集能力和介面變動風險。
- 每天何時產生報告、資料保留多久，以及通知與修正內容的具體呈現方式。
- GitHub 具體安裝命令、更新與移除流程；macOS 版本與硬體支援範圍，以及 Chrome 擴充功能如何安裝。

## 開發流程與目前狀態

採用 Josh 指定的 [AI 輔助開發流程](/Users/joshtsai/Documents/Josh_JobHunt/docs/ai-dev-process.md)：先立項且不寫程式；之後才做最薄的端到端骨架與驗證閘門；再以小任務、從規格推出的測試案例、實際驗證和風險 review 迭代。這份 `BRIEF.md` 是立項階段的第一份共用文件。`AGENTS.md` 已建立為共用規則檔，`CLAUDE.md` 僅指向它；`PROGRESS.md`、`TODO.md` 和程式碼尚未建立。不要把本 brief 當成已批准的架構設計。

舊的「AI 只教、Josh 全部自己寫」方式使先前兩個側專案停擺。這次在骨架階段，可以由 AI 先做出很薄、實際可跑的端到端版本；Josh 從它跑起來後接手修改與除錯。功能決策仍由 Josh 做。新觀念先給整體分節，再一次討論一節；每個任務用「現在看不到什麼 → 做完後會看到什麼」定義。AI 寫出的乾淨程式碼，不等於 Josh 已審查或驗證過它。
